import UIKit

final class {{ module_info.name }}Interactor {
    // MARK: - Private properties
    private weak var presenter: {{ module_info.name }}InteractorOutputProtocol?
}

// MARK: - {{ module_info.name }}InteractorInputProtocol
extension {{ module_info.name }}Interactor: {{ module_info.name }}InteractorInputProtocol {
    func setup(with output: {{ module_info.name }}InteractorOutputProtocol) {
        presenter = output
    }
}
