source "https://rubygems.org"

# Isolated from the repository-root CocoaPods bundle, which deliberately still
# supports macOS system Ruby 2.6. Fastlane 2.238.0 requires Ruby 3 or newer.
ruby ">= 3.2"
gem "fastlane", "2.238.0"
