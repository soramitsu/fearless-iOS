# Fearless iOS Codebase Critique

## Global singletons make composition and testing brittle
The composition roots instantiate collaborators by touching singletons (`KeychainManager.shared`, `LocalizationManager.shared`, `WalletConnectServiceImpl.shared`, `EventCenter.shared`, `ReachabilityManager.shared`, etc.). This tight coupling makes it difficult to inject fakes in unit tests and complicates modularisation. Extract explicit dependency containers or pass protocol-based collaborators into factories instead of resolving them statically.

Progress: URL handling now flows through `URLHandlingRegistryProtocol`, `URLHandlingDependencies`, and module-level dependency structs instead of feature assemblies calling the legacy shared keystore-import lookup directly. `FeatureToggleProvider` now receives its remote-config URL through `FeatureToggleConfigSource` instead of reading `ApplicationConfig.shared` internally; focused Debug and Release tests cover injected config URLs, missing URLs, nil remote snapshots, and released-provider fallback behavior. `WalletConnectDisconnectServiceImpl` now receives `WalletConnectService` through its initializer instead of directly resolving `WalletConnectServiceImpl.shared`; focused Debug and Release tests cover injected session disconnects and the no-session wallet-disconnect error path. `OnboardingService` now receives `OnboardingConfigSource` and `OnboardingConfigFetching` dependencies, removing direct `ApplicationConfig.shared` and `NetworkWorkerDefault` construction from the actor; focused Debug and Release tests cover missing URL and injected fetcher behavior. `WalletConnectServiceImpl` now receives `LocalizationManagerProtocol` through its initializer instead of reading `LocalizationManager.shared` inside invalid-URI handling; focused Debug and Release tests cover the injected localized error content. `SelectCurrencyInteractor` now receives the fiat-list URL through `SelectCurrencyConfigSource` instead of reading `ApplicationConfig.shared`; focused Debug and Release tests cover nil URL behavior and injected URL subscription wiring. `OKXDexAggregatorServiceImpl` now receives `OKXDexAggregatorConfigSource` instead of reading `ApplicationConfig.shared` for every request, and the dormant OKX request/response/service sources are wired into the app target; `OKXDexRequestSigner` now receives credentials and time through injectable providers instead of reading `OKXApiKeys` and `Date()` directly. `NomisAccountStatisticsFetcher` now receives `NomisAccountStatisticsConfigSource` instead of reading `ApplicationConfig.shared` for the score endpoint, and `NomisRequestSigner` now receives credentials through `NomisRequestSigningCredentialsSource` instead of reading `NomisApiKeys` directly. `AlchemyService` now receives its `NetworkWorkerDefault` and `AlchemyAPIKeySource` through its initializer, `AlchemyRequest` builds its authorized endpoint from that source instead of resolving `ThirdPartyServicesApiKeys` internally, and `AlchemyNFTOperationFactory` now uses the same injected key source with centralized NFT endpoint URL construction instead of reading the key enums directly. Etherscan-compatible and Oklink history factories now receive `BlockExplorerAPIKeySource` and build requests through testable helpers instead of reading `BlockExplorerApiKeys` directly; the Oklink request builder also appends required query items when the configured base URL has no existing query. `PricesService` now receives its price subscriber, logger, event center, and date provider through its initializer, and its currency aggregation no longer duplicates the first selected currency during subscription refreshes. `PriceDataSource` and `PriceProviderFactory` now accept explicit price-source dependencies, so event dispatch, Coingecko, Chainlink, SORA price fetching, and chain-registry access can be supplied locally instead of being resolved from shared singletons and concrete factories. `PriceLocalStorageSubscriberImpl` now accepts injected event-center, price-provider factory, and chain-repository fetcher dependencies and no longer carries an unused chain-registry singleton. Asset-network sort filters, NFT collection filters, and wallet transaction-history filters now build localized titles from explicit preferred languages, with presenters and assemblies passing their injected localization managers instead of those model types reading `LocalizationManager.shared` directly. Fixed-format remote-history timestamp parsing now uses an explicit POSIX parsing helper, so Alchemy, 5ire, Giantsquid, Zeta/Blockscout, and Arrowsquid history models no longer depend on shared or process-current localization state for network timestamps. `TonChainSelection.selectedChainId` now accepts an injectable toggle source instead of hard-wiring `LocalToggleService.shared` into the selection logic; `ChainRegistry` receives that toggle source through its initializer and factory dependency container instead of reading the shared service internally, and `TonJettonInjectorImpl` receives the same toggle source so price injection fetches the selected TON environment's chain through injected state. `GitHubPhishingServiceFactory`, `ScamSyncServiceFactory`, and `PolkaswapSettingsFactory` now receive explicit config, storage, operation, and fetcher dependencies, and `GitHubPhishingAPIService` now uses its injected operation factory instead of constructing a concrete `GitHubOperationFactory`. `SnapshotHotBootBuilder` now receives `SnapshotHotBootConfigSource` instead of reading chain-source URLs from `ApplicationConfig.shared` during hot boot. `AppVersionObserver` now receives `AppVersionConfigSource`, `AppSupportConfigFetching`, and callback queue dependencies instead of reading the app-version URL from `ApplicationConfig.shared` inside the version check. `PurchaseAggregator.defaultAggregator` now receives `PurchaseAggregatorConfigSource`, a Moonpay secret value, and a Moonpay provider factory explicitly, and it no longer builds default Ramp/Moonpay providers when custom providers are supplied. `SoraSubqueryPriceFetcherDefault` now receives `NetworkWorkerDefault` through its initializer instead of constructing one internally, and focused coverage exercises the SORA2 `https://pi.soramitsu.io/graphql` price path through an injected URLSession. `StakingRewardsFetcherAssembly` now receives a shared `NetworkWorkerDefault` and `LocalizationManagerProtocol`, and the Subquery, Subsquid, Giantsquid, SORA, and Reef staking reward fetchers all use the injected worker instead of constructing their own network clients; locale-sensitive fetchers also use the injected localization manager for date formatting. The older reward `RewardOperationFactory` now accepts an injected `LocalizationManagerProtocol` and passes it through SORA, Giantsquid, and Subsquid operation factories instead of those factories reading `LocalizationManager.shared` while building GraphQL timestamp filters. Focused Debug and Release compatibility tests cover injected OKX base URLs, OKX query-item wiring through the real `NetworkWorkerDefault` path, deterministic OKX signing headers, injected Nomis account-score URLs, deterministic Nomis credential headers, injected Alchemy request URLs through the real worker path, injected Alchemy NFT endpoint URLs, injected Etherscan/Oklink block-explorer credentials, injected `PricesService` throttle behavior, injected remote-sync factory URLs, injected SORA2 PI price fetching, injected `PriceDataSource` dependency wiring with SORA-over-Coingecko price replacement and currency event handling, injected price-local subscriber event-center and chain-sync refresh wiring, injected localized filter-title construction, deterministic network timestamp parsing, injected staking rewards fetcher networking for all supported staking explorer types, and injected reward-operation localization for SORA, Giantsquid, and Subsquid query generation; focused feature-toggle tests cover injected TON toggle selection, TON jetton price injection through the selected chain, plus the existing local-toggle persistence behavior, focused ChainRegistry tests cover injected TON environment selection during registry boot, focused runtime-sync tests cover injected snapshot hot-boot chain-source URLs, focused purchase-provider tests cover injected default-aggregator purchase configuration, and focused app-version tests cover injected config URLs, missing URL behavior, fetcher failures, and unsupported-version warning presentation. Remaining singleton cleanup should continue module-by-module for other non-URL services and factories such as event dispatch, localization, reachability, and secure-storage composition.
